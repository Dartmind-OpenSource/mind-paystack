### Enums & Constants

#### Environment

- `test`, `staging`, `production`

#### LogLevel

- `debug`, `info`, `warning`, `error`

#### ErrorCategory

- `network`, `authentication`, `validation`, `business`, `server`, `unknown`

#### ErrorSource

- `api`, `sdk`, `user`, `system`

#### ExceptionSeverity

- `fatal`, `error`, `warning`, `info`
